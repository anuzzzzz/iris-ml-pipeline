apiVersion: v1
kind: Service
metadata:
  name: iris-api-service
  namespace: iris-api
  labels:
    app: iris-api
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8081
    protocol: TCP
    name: http
  selector:
    app: iris-api
